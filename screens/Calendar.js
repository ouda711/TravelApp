import React from "react";
import { View, Text, Button } from "react-native";

const Calendar = () => {
    return (
        <View>
            <Text style={{flex: 1,justifyContent: "center", alignItems: "center"}}>Calendar</Text>
        </View>
    )
}

export default Calendar;