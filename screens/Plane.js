import React from "react";
import { View, Text, Button } from "react-native";

const Plane = () => {
    return (
        <View>
            <Text style={{flex: 1,justifyContent: "center", alignItems: "center"}}>Plane</Text>
        </View>
    )
}

export default Plane;